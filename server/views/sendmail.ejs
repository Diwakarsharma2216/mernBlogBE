<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Simple Beautiful UI</title>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
            background-color: #f0f4f8;
        }
        .container {
            text-align: center;
            background-color: #fff;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            max-width: 400px;
            width: 100%;
        }
        h2 {
            margin: 10px 0;
            color: #333;
        }
        h2:first-of-type {
            font-size: 24px;
        }
        h2:last-of-type {
            font-size: 22px;
            color: #4CAF50;
        }
    </style>
</head>
<body>
    <div class="container">
        <h2><%= name %></h2>
        <h2><%= otp %></h2>
    </div>
</body>
</html>
